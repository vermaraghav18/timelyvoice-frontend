// frontend/src/lib/rails.js
export const RAIL_WIDTH = 160;     // single truth (matches PublicHome + Finance)
export const RAIL_HEIGHT = 635;
export const RAIL_TOP_OFFSET = 0;

export const RAIL_GAP = 14;        // spacing between content and rail
export const RAIL_GUTTER = RAIL_WIDTH + RAIL_GAP;

// keep the same breakpoint you already use in SiteNav
export const RAILS_MIN_WIDTH = 1280;
